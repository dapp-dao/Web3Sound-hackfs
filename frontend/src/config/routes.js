const urls={
    HOME: '/',
    AUDIOSTORE: '/audiostore',
    PLAYER: '/player',
};

const routes={
    HOME: urls.HOME,
    AUDIOSTORE: urls.AUDIOSTORE,
    PLAYER: urls.PLAYER,
};

export default routes;